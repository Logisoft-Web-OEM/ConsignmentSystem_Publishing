import { Preset, definePreset } from 'unocss';

export default definePreset(() => {
  return {
    // name: 'logi-guide-preset',
    // shortcuts: [{}],
    // it supports most of the configuration you could have in the root config
  };
});
